apiVersion: v1
kind: Service
metadata:
  name: {{ include "j17-mvn-imp-def-bld-gh-mrg.fullname" . }}
  labels:
    {{- include "j17-mvn-imp-def-bld-gh-mrg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "j17-mvn-imp-def-bld-gh-mrg.selectorLabels" . | nindent 4 }}
