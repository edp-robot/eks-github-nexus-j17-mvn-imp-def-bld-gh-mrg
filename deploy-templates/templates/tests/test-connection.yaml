apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "j17-mvn-imp-def-bld-gh-mrg.fullname" . }}-test-connection"
  labels:
    {{- include "j17-mvn-imp-def-bld-gh-mrg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "j17-mvn-imp-def-bld-gh-mrg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
